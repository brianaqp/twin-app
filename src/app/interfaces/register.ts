export interface Register {
  vessel: string;
  flow: string;
  loadingPort: string;
  totalShipment: string;
  maxArrivalDraft: string;
  portCalls: object[];
  stowagePlan: object[];
  calados: object[];
}
